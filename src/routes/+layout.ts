// The whole app is a static site (SPEC.md §2) — prerender everything by default; individual
// routes can opt out later (e.g. milestone 5's "load your own book" loader).
export const prerender = true;
