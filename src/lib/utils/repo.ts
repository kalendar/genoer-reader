/**
 * Where this app's own source lives (SPEC.md §9 "Fork on GitHub" footer link,
 * §14 "Repo home — which GitHub org/repo hosts the app ... deferrable to
 * build time").
 *
 * TODO(repo-home): SPEC.md §14 leaves the repo home as an open decision — set
 * this to the real GitHub org/repo once one is chosen, before a public
 * deploy. Left as a placeholder pointing at the pipeline author's namespace
 * so the link is at least valid today rather than a dead `#`.
 */
export const REPO_URL = 'https://github.com/kalendar/genoer-reader';
